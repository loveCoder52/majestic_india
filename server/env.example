# Port the server will listen on
PORT=5000

# MongoDB connection string
# - For local MongoDB use: mongodb://127.0.0.1:27017/<dbname>
# - For MongoDB Atlas (SRV) use (example):
#   mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/<dbname>?retryWrites=true&w=majority
# Make sure your Atlas network access allows your IP (or 0.0.0.0/0 for testing) and DNS SRV lookups work locally.
MONGO_URI=mongodb://127.0.0.1:27017

# Token settings (replace with secure values in production)
ACCESS_TOKEN_SECRET=replace-with-strong-secret
ACCESS_TOKEN_EXPIRY=15m
REFRESH_TOKEN_SECRET=replace-with-refresh-secret
REFRESH_TOKEN_EXPIRY=7d

# CORS origins allowed (comma separated)
CORS_ORIGIN=http://localhost:5000,http://localhost:5173

